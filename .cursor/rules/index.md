Important:
* please read `index.md` in each child folders in `.project-rules`
* the `index.md` will is a route document to guide what we need to read
* not only read `index.md` but read related specific rules reference in `.project-rules/{childs_folder}/index.md`